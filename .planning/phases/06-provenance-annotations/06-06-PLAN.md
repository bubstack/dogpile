---
phase: "06"
plan: "06"
type: execute
wave: 3
depends_on:
  - "06-02"
  - "06-03"
  - "06-04"
files_modified:
  - CHANGELOG.md
  - CLAUDE.md
autonomous: true
requirements:
  - PROV-01
  - PROV-02

must_haves:
  truths:
    - "CHANGELOG.md has a v0.5.0 entry covering all five breaking/additive changes from D-14"
    - "CHANGELOG entry lists: model-request/response now emitted (behavioral addition)"
    - "CHANGELOG entry lists: ModelRequestEvent drops at, gains startedAt + modelId (breaking shape change)"
    - "CHANGELOG entry lists: ModelResponseEvent drops at, gains startedAt + completedAt + modelId (breaking shape change)"
    - "CHANGELOG entry lists: ConfiguredModelProvider.modelId? added (additive)"
    - "CHANGELOG entry lists: ReplayTraceProviderCall.modelId added (additive)"
    - "CHANGELOG entry lists: new /runtime/provenance subpath (additive)"
    - "CHANGELOG entry includes migration note for callers with exhaustive switches"
    - "CLAUDE.md cross-cutting invariants section updated to mention ModelRequestEvent/ModelResponseEvent shape"
  artifacts:
    - path: "CHANGELOG.md"
      provides: "v0.5.0 provenance annotations entry"
      contains: "model-request"
    - path: "CLAUDE.md"
      provides: "Updated public-surface invariant chain"
      contains: "ModelRequestEvent"
---

<objective>
Write the CHANGELOG entry for Phase 6 per D-14, and update the CLAUDE.md cross-cutting invariants to mention the new event shapes. This completes the public-surface lockstep chain required by every public-surface change in this project.

Purpose: Per the project's public-surface invariant (CLAUDE.md), every event/result/exports change updates CHANGELOG.md and CLAUDE.md. Without this plan, the Phase 6 changes are undocumented and the invariant is broken.
Output: CHANGELOG v0.5.0 entry; updated CLAUDE.md invariants.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-provenance-annotations/06-CONTEXT.md
@.planning/phases/06-provenance-annotations/06-02-SUMMARY.md
@.planning/phases/06-provenance-annotations/06-03-SUMMARY.md
@.planning/phases/06-provenance-annotations/06-04-SUMMARY.md

<interfaces>
<!-- D-14 CHANGELOG items (from CONTEXT.md) -->
1. Behavioral addition: model-request and model-response events are now emitted as real runtime events in trace.events and the streaming event log
2. Breaking shape change: ModelRequestEvent drops `at`, gains `startedAt: string` and `modelId: string`
3. Breaking shape change: ModelResponseEvent drops `at`, gains `startedAt: string`, `completedAt: string`, and `modelId: string`
4. Additive: ConfiguredModelProvider gains optional `modelId?: string`
5. Additive: ReplayTraceProviderCall gains `modelId: string`
6. Additive: New /runtime/provenance subpath export with getProvenance(), ProvenanceRecord, PartialProvenanceRecord

<!-- Migration note pattern from v0.4.0 sub-run-* events (CONTEXT.md D-06) -->
"model-request and model-response events are now emitted; update exhaustive switches if you relied on them never appearing"

<!-- CLAUDE.md public-surface invariant section to update -->
"Replayable trace contract: completed runs return JSON-serializable traces... Event-shape changes are public-API changes — update src/tests/event-schema.test.ts, src/tests/result-contract.test.ts, and the changelog."
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write CHANGELOG.md v0.5.0 provenance annotations entry</name>
  <read_first>
    - CHANGELOG.md (beginning of file — find the most recent entry to understand format; look for v0.4.0 entry as the format template)
  </read_first>
  <files>CHANGELOG.md</files>
  <action>
Read CHANGELOG.md to understand the existing format and version heading pattern. Then add a v0.5.0 section at the top (before v0.4.0), following the same heading, sub-heading, and bullet format.

The entry must cover all six items from D-14. Use the following structure (adapt to match the existing CHANGELOG format):

```markdown
## v0.5.0 — Observability and Auditability

### Phase 6: Provenance Annotations

#### Breaking Changes

- **`ModelRequestEvent` shape changed.** The `at` field is removed. The event now carries `startedAt: string` (ISO-8601 timestamp immediately before the provider call) and `modelId: string` (resolved model identifier). Update any code that reads `event.at` on a `ModelRequestEvent`.

- **`ModelResponseEvent` shape changed.** The `at` field is removed. The event now carries `startedAt: string`, `completedAt: string` (ISO-8601 timestamp after the provider call), and `modelId: string`. Callers can compute call duration from a single event. Update any code that reads `event.at` on a `ModelResponseEvent`.

- **`model-request` and `model-response` events are now emitted.** These event types were previously typed but never produced at runtime. They are now emitted on every provider call across all four protocols (`sequential`, `broadcast`, `coordinator`, `shared`). Callers with exhaustive switches over `RunEvent["type"]` that lack a `default` branch may encounter unhandled cases — add `case "model-request":` and `case "model-response":` branches or a fallback `default`.

#### Additions

- **`ConfiguredModelProvider.modelId?` (optional field).** Provider adapters can now declare the specific model identifier (e.g., `"gpt-4o"`). When absent, the SDK uses `provider.id` as the fallback. `createOpenAICompatibleProvider` and `createVercelAIProvider` populate this field automatically from the constructor argument.

- **`ReplayTraceProviderCall.modelId` (new required field).** The model identifier is now recorded in every provider call entry in `trace.providerCalls`. This is a shape change on the replay type — if you have hand-crafted `ReplayTraceProviderCall` objects (e.g., in tests), add the `modelId` field.

- **New subpath: `@dogpile/sdk/runtime/provenance`.** Exports `getProvenance(event)`, `ProvenanceRecord`, and `PartialProvenanceRecord`. `getProvenance()` extracts normalized provenance fields from any `ModelRequestEvent` or `ModelResponseEvent`. The overloaded signature returns `ProvenanceRecord` (with `completedAt`) for response events and `PartialProvenanceRecord` (without) for request events.

#### Replay

- **`replay()` synthesizes `model-request` / `model-response` events from `trace.providerCalls`.** The augmented event log returned by `replay()` includes provenance events derived from the canonical `providerCalls` anchor. This ensures provenance fields in replayed results are identical to those in live runs (PROV-02). Older traces without these events in `trace.events` gain them on replay.
```

Place this v0.5.0 section at the top of the file, before the existing v0.4.0 entry. Match whatever heading level and formatting style is used for v0.4.0.
  </action>
  <verify>
    <automated>grep -c "model-request" CHANGELOG.md</automated>
  </verify>
  <done>
    - grep -c "v0.5.0" CHANGELOG.md returns at least 1
    - grep -c "ModelRequestEvent" CHANGELOG.md returns at least 1
    - grep -c "ModelResponseEvent" CHANGELOG.md returns at least 1
    - grep -c "runtime/provenance" CHANGELOG.md returns at least 1
    - grep -c "exhaustive" CHANGELOG.md returns at least 1 (migration note present)
    - grep -c "ReplayTraceProviderCall" CHANGELOG.md returns at least 1
  </done>
</task>

<task type="auto">
  <name>Task 2: Update CLAUDE.md cross-cutting invariant chain</name>
  <read_first>
    - CLAUDE.md (find the "Replayable trace contract" section and "Recursive coordination public-surface mirror" section)
  </read_first>
  <files>CLAUDE.md</files>
  <action>
Read CLAUDE.md and locate the "Replayable trace contract" cross-cutting invariant. It currently reads (paraphrased):

"completed runs return JSON-serializable traces... Event-shape changes are public-API changes — update `src/tests/event-schema.test.ts`, `src/tests/result-contract.test.ts`, and the changelog."

Update this section to mention that `ModelRequestEvent` and `ModelResponseEvent` now carry `startedAt`/`completedAt`/`modelId` instead of `at`. Add a note that the provenance event shape is additionally protected by `src/tests/provenance-shape.test.ts` and the frozen fixture at `src/tests/fixtures/provenance-event-v1.json`.

The specific text to add/update (adapt to match surrounding prose style):

After the existing sentence about event-shape changes being public-API changes, add:
"**`ModelRequestEvent` and `ModelResponseEvent` use `startedAt`/`completedAt`/`modelId` instead of `at`.** These event types no longer carry the `at` field. The provenance shape is additionally protected by `src/tests/provenance-shape.test.ts` and `src/tests/fixtures/provenance-event-v1.json` — update the fixture when intentionally changing the provenance event shape."

Do NOT restructure or reformat the rest of CLAUDE.md — make only the targeted addition to the cross-cutting invariants section.
  </action>
  <verify>
    <automated>grep -c "provenance-event-v1.json" CLAUDE.md</automated>
  </verify>
  <done>
    - grep -c "provenance-event-v1.json" CLAUDE.md returns 1
    - grep -c "startedAt" CLAUDE.md returns at least 1
    - CLAUDE.md is valid Markdown (no syntax errors introduced)
    - pnpm run typecheck exits 0 (CLAUDE.md change doesn't affect compilation)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none | Documentation changes only; no runtime code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06 | Information Disclosure | CHANGELOG | accept | CHANGELOG is public; model identifier and event shape descriptions are intentional API documentation |
</threat_model>

<verification>
After both tasks complete:
- `grep -c "v0.5.0" CHANGELOG.md` returns at least 1
- `grep -c "exhaustive" CHANGELOG.md` returns at least 1 (migration note)
- `grep -c "provenance-event-v1.json" CLAUDE.md` returns 1
- `pnpm run typecheck` exits 0 (verify documentation changes introduced no issues)
</verification>

<success_criteria>
CHANGELOG has a v0.5.0 Phase 6 entry covering all six D-14 items. CLAUDE.md cross-cutting invariants mention the new event shapes and the provenance fixture. The public-surface lockstep chain is complete.
</success_criteria>

<output>
After completion, create `.planning/phases/06-provenance-annotations/06-06-SUMMARY.md`
</output>
